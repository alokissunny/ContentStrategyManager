/*
 * Demo defaults the Visual Brand reads from.
 *
 * Ported with the Visual Brand pages: the only thing Visual Style pulls from
 * here is the studio's default typefaces, used to seed the FontsField before
 * the studio hands over their own font files. The reference's full demo dataset
 * (personas, projects, plans) is not needed — the live app has real data.
 */

export const DEMO = {
  brand: {
    fonts: 'Bricolage Grotesque for headlines, Inter for body text.',
  },
};
