VITE_API_URL=http://localhost:5000/api
VITE_GOOGLE_CLIENT_ID=
# Optional. CloudFront (or custom CDN) origin for project media, e.g.
# https://d111abcdef8.cloudfront.net — used for <img> display. Leave blank to
# read it from the API (GET /media/cdn-base, which mirrors MEDIA_CDN_BASE_URL).
VITE_MEDIA_CDN_BASE_URL=
